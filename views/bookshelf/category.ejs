<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/book.css">
</head>

<body>
    <%- include('../includes/navigation.ejs') %>

    <main>
        <% if (boks.length > 0) { %>
        <div>  
            <% for (let book of boks) { %>
        <li>
            <a href="/<%= book.category %>"><%= book.category %>
            </a>
        </li>

            <% } %>
        </div>
    <% } else {%>
        <h1>No Book Found!</h1>
    <% } %>
    </main>   
<%- include('../includes/end.ejs') %>